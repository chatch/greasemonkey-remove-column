greasemonkey-remove-column
==========================

Remove a column from an HTML table.

Change table selector and column index as required.

This script takes the phonetic column out of thai-language.com dictionary results.
